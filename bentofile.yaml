service: "examen_bentoml.service:Prediction"

description: "file: ./README.md"

labels:
   owner: "DridrM"
   project: "Examen BentoML"

docker:
  base_image: "python:3.12-slim"

include:
  - "src/"

models:
  - "elasticnet-bento-exam:latest"

python:
  packages:
    - "jwt>=1.4.0"
    - "pandas>=2.3.2"
    - "scikit-learn>=1.7.1"

envs:
  - name: "JWT_SECRET"
    value: ${JWT_SECRET}